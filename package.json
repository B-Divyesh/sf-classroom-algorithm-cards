{
  "name": "classroom-algorithm-cards",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/emit-swa-config.mjs",
    "check:artifact-policy": "node scripts/check-artifact-policy.mjs",
    "test": "vitest run && npm run build && npm run check:artifact-policy",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "axe-core": "^4.10.3",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
